<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.bem_vindo.BemVindoActivity">

    <ImageView
        android:id="@+id/imagemBackground"
        android:layout_width="match_parent"
        android:layout_height="330dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="40dp"
        android:adjustViewBounds="false"
        android:background="@color/primary"
        android:contentDescription="@string/plano_de_fundo"
        android:cropToPadding="false"
        android:scaleType="fitStart"
        android:scaleX="1.4"
        android:scaleY="1.3"
        app:srcCompat="@drawable/ilustra__o_bem_vindo_tupi"
        tools:ignore="VisualLintBounds" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="324dp"
        android:layout_below="@id/imagemBackground"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="54dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textBemVindo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginVertical="30dp"
            android:layout_marginTop="140dp"
            android:layout_marginBottom="-166dp"
            android:gravity="center"
            android:text="@string/bem_vindo_vamos_viajar"
            android:textAlignment="center"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"
            android:textSize="32sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/buttonComecar"
            android:layout_width="match_parent"
            android:layout_height="73dp"
            android:layout_marginStart="55dp"
            android:layout_marginEnd="56dp"
            android:layout_marginBottom="40dp"
            android:clickable="true"
            android:onClick="onHomeScreen"
            android:text="@string/comecar"
            android:textSize="20sp"
            tools:ignore="VisualLintButtonSize" />
    </LinearLayout>

</RelativeLayout>